"""Single source of truth for the app version.

Bump this on every release users would notice; the sidebar version stamp and
anything else that needs the number read it from here.
"""

__version__ = "2.1.0"
